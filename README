转换T-Moble主题到MagicMod的主题

此工具只是一个很初略的版本,会自动转换相应的图片,颜色定义等为MagicMod支持的格式
受限于系统,转换后的主题显示并不能100%和原版相似,需要更多的微调


执行 ./gen.sh $主题apk名即可自行转换

gen_map.sh 用来生成相应的资源map,此处已经带有相关的map,无需转换
copyFile.sh 用于搜索相关的文件并复制到目标目录(此工具已作废)
